// Draw a Rectangle

/* function drawRectangle (length, width) {
    for (let i = 0; i < width; i++) {
        const row = '*'.repeat(length); 
        console.log(row)
    }    
}
drawRectangle(7, 4) */