const batteryIsWorking = false
const engineIsWorking = true
const carIsMagical = true

if (batteryIsWorking && engineIsWorking || carIsMagical) {
    console.log('The car is working!')
} else {
    console.log('The car needs to be repaired!')
}